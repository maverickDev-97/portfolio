import { FC } from 'react'
import { StyledButton } from './Button.styles';

const Button: FC = () => {
    return (
        <StyledButton>
            Click me
        </StyledButton>
    )
}

export default Button;